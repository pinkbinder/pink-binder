/** Posts shown before the grid's "load more" sentinel takes over. */
export const BLOG_INDEX_INITIAL_COUNT = 9

/** Fallback grid thumbnail when a post has no resolvable hero artwork. */
export const DEFAULT_POST_THUMBNAIL = '/images/logo.png'
