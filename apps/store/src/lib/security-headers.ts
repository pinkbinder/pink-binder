import { applySecurityHeaders as _applySecurityHeaders } from '@repo/config'

/** Shared security headers helper (re-exported from `@repo/config`). */
export { _applySecurityHeaders as applySecurityHeaders }
