# Landing app — set local values in apps/landing/.env.local.
# Deployed secrets live in the Cloudflare Worker dashboard.
#
# See repo root `.env.example` for the full variable reference (eBay, Etsy, Whatnot, etc.).
