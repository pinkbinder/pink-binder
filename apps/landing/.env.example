# Landing app — pull env from Vercel (do not hand-copy secrets):
#
#   cd apps/landing && vercel link && vercel env pull .env.local
#
# See repo root `.env.example` for the full variable reference (eBay, Etsy, Whatnot, etc.).
