---
import { renderAuthoredMdxHtml } from '../lib/authored-mdx'

interface Props {
  content: string
}

const { content } = Astro.props as Props
const html = renderAuthoredMdxHtml(content)
---
<div class="mdx-prose" set:html={html} />
