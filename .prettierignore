# Generated release metadata is intentionally managed by Release Please.
CHANGELOG.md

# Runtime files are checked out under .github during CI. Composite actions
# are copied to .github/actions for the workflow run and must not be policed
# by consumer formatters (their style follows the runtime baseline, not the
# consumer .prettierrc).
.github/.code-foundry
.github/actions/

# Repository-specific rules
# Runtime files are checked out under .github during CI.
.next
.open-next
.wrangler
next-env.d.ts
node_modules
.code-foundry
dist
build
coverage
.turbo
*.lock
*.env.example
pnpm-lock.yaml
# Generated Pokémon / TCG JSON cache (hand-edited source lives in src/ and scripts/)
packages/data/cache/
apps/blog/public/data/card-galleries/
apps/blog/public/data/expansion-galleries/
apps/blog/public/_headers
