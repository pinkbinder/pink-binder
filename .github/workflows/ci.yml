name: CI

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint, type-check, and format
        run: pnpm validate

      - name: Audit generated publishing data
        run: pnpm --filter @repo/data audit:blog-publishing

      - name: Build blog
        run: pnpm exec turbo run build --filter=@repo/blog

      - name: Crawl every compiled blog route
        run: pnpm --filter @repo/blog audit:routes
